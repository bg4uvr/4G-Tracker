;    4G-Tracker
;    （一款使用 4G DTU 制作的 APRS Tracker，by BG4UVR 2022.6.2）
;    开源网址：
;    https://gitee.com/bg4uvr/LTE-Tracker
;    https://github.com/bg4uvr/4G-Tracker

;                   --------------------------
;                   |  4G-Trakcer 配置文件  |
;                   --------------------------
;
; 行首以分号开头为注释行，如需设置请删除相应配置行首的分号。只有呼号和验证码为必设项，
; 其他参数项均为有默认值的选设项，所以只要设置好呼号和验证码，系统就已经可以正常工作。

; 注意：如果某参数使用默认配置，那么并不需要删除行首的分号，只有更改配置才需要删除之。

; 呼号，必须设置，否则系统不工作
CALLSIGN        =   BGnXXX

; ARPS验证码，必须设置，否则系统不工作
PASSCODE        =   12345

; SSID，1-2个数字或字母。默认值 4G
;SSID           =   4G

; 台站图标TABLE。默认值 /
;TABLE          =   /

; 台站图标SYMBOL。默认值 >
;SYMBOL         =   >

; APRS服务器地址，域名或IP地址都可以。默认值 china.aprs2.net
;SERVER         =   china.aprs2.net

; APRS服务器端口号，范围 1024-49151。默认值  14580
;PORT           =   14580

; 信标文本，最长62个字符。
;BEACON         =   4G-Tracker

; 信标发送间隔，单位分钟，范围 10 - 600 默认值 60，设置为 0 时关闭信标发送功能
;BEACON_INTERVAL=   60
